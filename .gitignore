# Rust build artifacts
/target/

# Node (ESLint dev tooling for public/js — no frontend build step, just linting)
/node_modules/
# Cargo.lock is tracked — this is a binary (src/main.rs), not a library,
# so the lockfile should be committed for reproducible builds.

# Claude Code internal state
.claude/

# Runtime state
*.db
*.db-shm
*.db-wal
in_flight.json
vapid_key.pem

# Éditeurs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Scratch data-analysis workspace (DB snapshots, one-off scripts, generated reports)
/analysis/